# RESUME — Apologetics Library Build (handoff for a fresh session)

State as of 2026-06-16 (**Rounds 2–5 COMPLETE** + quality pass + SOUL/Protocol routine upgrade + an 11-video build round; **git index was corrupted → repaired, all work synced**). The vault is git-backed + auto-pushed by `sync-advocate-loop.ps1`. To continue in a NEW session, read this file first. (Portable copy: `C:\Users\Josep\Downloads\Apologetics-Library-RESUME.md`.)

## ▶ PICK UP HERE (next session)
1. ✅ **Forward-link pass — DONE (6/16).** Phantom/near-miss links resolved across waves; final batch wrote 33 notes (21 Bible/D&C + 10 Book-of-Mormon scriptures + `Filioque` + `curse of Canaan`) and added the `Sources` hub (`Sources/Sources.md`, mirrors `Concepts/Concepts.md`; 92 breadcrumbs link it). **Vault now: 0 empty files, 0 genuine dangling links.** `analyze-broken-links.py` residue is all false-positive — C near-miss 0; D phantom = only the 8 `[[Consolidation Report]]` scan-artifacts (that file lives in 99-meta, excluded from the scan, resolves fine in Obsidian); B alias 155 (resolve via note aliases). NEW RULE now in every fill-agent prompt: **only wikilink a note that already exists** (else plain text) — this is what finally stopped the forward-link cascade. STILL TRUE: category **C is UNRELIABLE for scriptures** (fuzzy-pairs different verses, e.g. `Matthew 5.39`→`5.13`) — create the verse note, never auto-retarget.
2. ✅ **Concept dupes — DONE.** Merged 4 of the 18 candidates (After-All-We-Can-Do Despite→In-Spite-of; Judas Problem→Argument; Fourfold-Medieval→Fourfold Sense; Appeal-to-Mystery short→long-of-Meaning), each canonical got the dup title as an alias + all backlinks repointed. The other 14 are legitimately distinct (NDE-rebuttal cluster; Word-of-God lexical-vs-epistemic; Peterson-inter-religious vs Becerra-patristic theosis; the two Alma-24 chiasms; the two Stowell events; etc.) — do NOT merge.
3. ✅ **3 debate maps — DONE:** `Concepts\Debate - Book of Mormon Historicity.md`, `Debate - BoM Witnesses.md`, `Debate - Problem of Hell.md` (63/52/70 verified links; same format as the Joseph-Smith / Trinity / Sola-Scriptura maps).
4. ✅ **Mahonri refreshed (6/16 00:17).** Only ONE process is the apologist gateway (its cmdline has `-p apologist`); the plain `hermes.exe gateway run` PID is the OTHER bot (Jarvis on `hermes-brain`) — LEAVE IT. Stopped the stale apologist PID, relaunched `scripts\run-apologist-gateway.ps1`; `profiles\apologist\logs\gateway.log` confirms `Connected as Mahonri#1216`. Discord ASK now reflects the full library.
5. ⏳ **DOMAIN-SHARD `Concepts/` — Joseph APPROVED 6/16; do as its own focused pass (taxonomy awaiting final OK).** GitHub truncates a folder's web listing at 1,000 files and `Concepts/` has 2,394 — data is 100% safe (git/clone/Obsidian unaffected), browser-only. Move notes into ~14 domain subfolders: book-of-mormon, joseph-smith, restoration-history, temple-priesthood, bible-canon, god-trinity, problem-of-evil, natural-theology, method-epistemology, salvation-grace, ethics-culture, other-religions, debate-maps, reference. Obsidian resolves `[[links]]` by basename so links survive IF basenames stay globally unique. Tags are fragmented (`#apologetics/X` + `#topic/X`) → build a curated tag→domain map + agent-classify the residue; re-run regen-index + analyze-broken-links after. (Scriptures 664 / People 886 are under 1,000 for now.)
6. ⏳ **Other quality:** **People dups** still pending (`Jack Welch`↔`John W. Welch`, `Richard Bushman`↔`Richard Lyman Bushman`, `Daniel C. Peterson`↔`David Peterson`, `Larry Murf`↔Murph — **Givens already merged 6/16** to `Terryl Givens`). **Semantic index** (`nomic-embed-text` unused). **TF Levels 2–4** blocked on Joseph. New-debate-map candidates flagged in `99-meta/Open Questions.md`: Theosis, Free Will, Kalam/God's existence.
7. ⚠ **Git/sync gotcha (6/16):** the `.git/index` corrupted (valid `DIRC` header but a bad entry → `fatal: unknown index entry format`), and `sync-advocate-loop.ps1` **fails SILENTLY** on it (errors are `2>$null`d) — it had been dead since the night before with no commits. Fix: stop the loop, `rm .git/index`, `git read-tree HEAD` (working tree untouched), commit+push, relaunch the loop. If GitHub looks stale, check `git -C <vault> status` works and that the loop PID is alive.

## Where things stand
**Sources 337 · Concepts 2,394 · Scriptures 664 · People 886 · Authority Ledger 580 scholars · 7 debate maps.** Rounds 2–4 (videos + PDFs/articles); **6/16 quality pass** (forward-links → 0 empty/0 dangling, dupes merged, 3 debate maps, Mahonri refreshed); **6/16 Round 5 build** (+11 videos: 7 "caption-disabled" recovered via yt-dlp json3 + WARD RADIO Pace-memo/SRA Parts 3–6) with debate-map upkeep (updated Sola Scriptura/Trinity/Problem-of-Hell, + new **Debate - LDS Satanic Ritual Abuse Allegations** map). Course hub: [[Thoughtful Faith Apologetics Course]].

## The pipeline (all reusable; scripts in `C:\Users\Josep\AppData\Local\hermes\scripts\`)
Python venv: `C:\Users\Josep\AppData\Local\hermes\hermes-agent\venv\Scripts\python.exe`
1. **Queue:** canonicalize URLs to `https://youtu.be/<id>` (prefetch regex misses `/shorts/`). Dedup: `for id in $ids; do grep -rqF -- "$id" Sources/ && echo BUILT || echo NEW; done`.
2. **Prefetch:** `prefetch-transcripts.py <N> <queuefile>` → `scripts\transcripts\<vid>.txt`. **~16–22 fetches per Proton IP before YouTube blocks**; rotate to a fresh/SMALL-country server (Belarus etc. least blocked; US/Canada re-block fast). `yt-dlp` is NOT a fallback (429). Put low-priority vids EARLY. oEmbed `get-yt-meta.py --cached-only` works under a block. "Subtitles are disabled" = caption-disabled, unbuildable, skip.
3. **Build (parallel):** general-purpose agents, one per cached vid. ⚠ **TWO failure modes learned in Round 4:** (a) the whole machine routes through Proton, so a VPN rotation mid-build kills in-flight agents (socket/ECONNRESET) — **prefetch ALL first, then hold the VPN steady (or disconnect) during builds**; (b) **>~12-16 concurrent agents triggers an Anthropic server rate-limit** ("temporarily limiting requests") that fails the tail of a wave — **keep waves ≤~8, or use 4-agent batches when re-spawning.** Each agent: READ cached transcript (no internet), write deep notes to FLAT folders; SINGLE-bracket cites `[^n]: [[Source|Title]], [MM:SS], <url>` (NEVER `[[MM:SS]]` — sweep `grep -rlE '\[\[[0-9]{1,3}:[0-9]{2}'` must be 0); ≥2 wikilinks; existing Scripture/People → wikilink only; foils get `## Apologetic Engagement`/`## LDS Relevance`; NEVER touch `99-meta\`,`MOCs\`,`Glossary\`,`Sources Index.md`, other sources. Glob FAILS from agent cwd → Bash `ls`/Grep with ABSOLUTE paths + FORWARD SLASHES. **After a big run, gap-check:** loop queue IDs, grep `Sources/`, re-spawn missing. **Content filter:** quoting graphic violence (e.g. the Nephi/Laban account) can block an agent's OUTPUT mid-run — tell scripture agents to summarize violence clinically and keep the final report terse.
4. **Books/articles:** `get-pdf-text.py <pdf> <first> <last>` (`[p.N]` cites); `get-article.py <url>` (trafilatura). One thorough agent for a 20-70pp essay; chapter agents for big books.
5. **Housekeeping each wave:** `regen-index.py`; normalize split tags (`tradition/critical-scholarship`→`tradition/academic-biblical-studies`); `[[MM:SS]]` sweep; merge dup People; `consolidation-scan.py` + `analyze-broken-links.py`. **Forward-link fill:** `analyze-broken-links.py` category D ≥3-backlinks = stub candidates → spawn agents to write them (scriptures = verse+gloss+backlinks; concepts = grep linkers, reconstruct claim; people = bio). **CRITICAL fill rule (6/16): fill-agents must ONLY wikilink notes that already exist (else plain text) — otherwise each fill spawns fresh forward-links and the long tail never converges.** Then delete any 0-byte root files Obsidian created from clicked links (`find . -maxdepth 1 -name "*.md" -size -3c -delete`). **Click-ghosts:** clicking an unresolved `[[link]]` materializes an empty exact-name root file that then SHADOWS any matching alias (e.g. `[[James E. Talmage]]` masked the real `People/James Talmage.md` which already aliases that name) — just delete the 0-byte root file.

## PENDING WORK
**A. ✅ COMPLETE — books/docs:** the 6 TF L1 PDFs + Fife + BYU Evolution + grace handout + Hayden Ratio Christi + Paulsen *Problem of Evil* + Ostler *Out of Nothing* (ex-nihilo history) + Ostler *Atonement in Mormon Thought* (compassion theory) + Hollis Johnson *Big Bang* + Apotheosis *Ex Materia* blog + BYU *leave-religion* article.
**B/C. ✅ DONE — videos:** Round 1 + Round 2 (56) + Round 3 (59/60, 1 caption-disabled `FpF9o0-8ZKk`) + **Round 4 (44/44)** — Stick of Joseph (temple/scripture deep-dives, King Follett, brass plates, 7 feasts), Ward Radio, Hayden Carroll, Keystone, Thoughtful Faith debates, Gavin Ortlund, Craig vs Goff, Benjamin Park, Counsel of Trent, Murph, Ether's Elephant, Katy Faust, etc.
**D. ✅ DONE — 3 debate maps.** **E. ✅ DONE — forward-links (0 empty / 0 dangling) + 4 concept dupes merged.** ⏳ remaining quality: People dups, semantic index, domain sub-MOCs.

## Also pending
- **Mahonri:** ✅ refreshed 6/16. To re-refresh after future waves: stop the `-p apologist` PID only (NOT the plain-`gateway run` Jarvis PID), relaunch `scripts\run-apologist-gateway.ps1`.
- TF course **Levels 2, 3, 4** not yet provided by Joseph.
